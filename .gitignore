.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
target/

# Third-party OpenAPI specs — downloaded, not authored. See research/scripts/fetch_specs.py

# Raw benchmark run logs (regenerable; may contain test-mode API object data).
# The committed results/*.md summaries are the citeable evidence.
.DS_Store
.claude/settings.local.json

# Local integration playground (fetched specs, machine-specific paths)
demo/

# Private lab material — research, notebook, and internal drafts (not for public release)
research/
planning/
LABBOOK.md
# Private drafts still living in docs/. Prefer moving new private material into
# planning/ or research/ — a per-file rule here silently swallows a legitimate
# public doc that later takes the same name.
docs/benchmarks.md
docs/ROADMAP.md
examples/stripe-naive-proxy.yaml
examples/stripe-faithful-proxy.yaml
# Downloaded OpenAPI specs placed next to example configs
examples/*.json
